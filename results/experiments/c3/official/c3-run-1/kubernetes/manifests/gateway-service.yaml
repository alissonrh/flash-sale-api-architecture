apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: flash-sale
  labels:
    app: gateway
    scenario: c3
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: proxy
      port: 8000
      targetPort: proxy
